Android officially does not support any API to implement this task :v (obviously). I had to use some treats and search on internet, one of them is "java reflection". Only can run with my device because I use some specicial settings in my phone.
